#---------------------------------------------------------------------------------------------------#
# Pacakges required to run code to manipulate, analyze and output data for#
# the Pacific Salmon Foundation's Pacific Salmon Explorer. Last updated  ##    
# June 6, 2016 	###############################################
#---------------------------------------------------------------------------------------------------#

# packages must be installed first. If it is the first time you are using the package on your computer install packages via "instal.package()"

require(reshape2) # for manipulating output into format required for the PSE 
require(zoo) # for rollspply function
require(runjags) # for Bayesian trend estimate
require(R2jags) # for Bayesian trend estimate
require(reshape2) # for manipulating outut into required format 
